name: extract_model
channels:
  - conda-forge
dependencies:
  # Required for full project functionality (dont remove)
  - pytest
  # Examples (remove and add as needed)
  - cf_xarray>=0.6
  - cmocean
  - dask
  - matplotlib
  - netcdf4
  - numpy
  - pip
  - requests
  - scipy
  - xarray
  - xcmocean
  - xesmf
