name: extract_model
channels:
  - conda-forge
dependencies:
  # Required for full project functionality (dont remove)
  - pytest
  - cf_xarray>=0.6
  - cmocean
  - dask
  - matplotlib
  - netcdf4
  - numpy
  - pip
  - pyinterp
  - requests
  - scipy
  - xarray
  - xcmocean
  - xesmf
